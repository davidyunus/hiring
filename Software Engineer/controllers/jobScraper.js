const puppeteer = require('puppeteer')
const Jobs = require('../models/jobs')

module.exports = {
    async jobScraper (req, res, next) {
        try {
					
        } catch (err) {

        }
    }
}